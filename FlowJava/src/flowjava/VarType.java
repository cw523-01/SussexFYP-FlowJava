package flowjava;

/**
 * enumerator for valid variable types in a flowchart
 *
 * @author cwood
 */
public enum VarType {
    STRING,
    BOOLEAN,
    CHARACTER,
    INTEGER,
    DOUBLE,
    FLOAT,
    LONG,
    SHORT
}
